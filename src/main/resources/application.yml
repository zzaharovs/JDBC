spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
#  sql:
#    init:
#      schema-locations: classpath:/schema-delete.sql
#      encoding: UTF-8
#      mode: always